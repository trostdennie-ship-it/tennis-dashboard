/* =================================================================
   Konfiguration (optional)
   -----------------------------------------------------------------
   Gute Nachricht: Diese App braucht KEINEN API-Schlüssel.

   Die Weltrangliste (ATP & WTA) wird einmal täglich kostenlos und ganz
   ohne Anmeldung aus einer offenen Datenquelle aktualisiert
   (siehe scripts/update-rankings.mjs + .github/workflows/update-rankings.yml).
   Die App liest dann einfach die Dateien data/rankings-atp.json bzw.
   data/rankings-wta.json.

   Diese Datei ist nur noch ein Platzhalter für eventuelle spätere
   Erweiterungen – du musst hier nichts eintragen.
   ================================================================= */
window.TENNIS_CONFIG = {};
